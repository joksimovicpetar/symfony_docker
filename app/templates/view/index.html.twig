{% extends 'base.html.twig' %}


{% block body %}
    <h1>o.o {{d}}</h1>
    <h1>{{user.age}}</h1>

    <ul>
        {% for data in a %}
            <li>{{data}}</li>
        {% endfor %}
    </ul>
{% endblock %}
